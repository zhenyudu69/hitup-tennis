export default function PrivacyPolicyLayout({ children }: { children: React.ReactNode }) {
  return (
    <main className="prose prose-slate mx-auto px-4 py-8 lg:prose-lg">
      {children}
    </main>
  );
}
